<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Colori di sfondo e primo piano -->
    <Color x:Key="BackgroundColor">#FF1E1E1E</Color>
    <!-- Grigio molto scuro -->
    <Color x:Key="ForegroundColor">#FFF1F1F1</Color>
    <!-- Bianco leggermente meno acceso -->

    <!-- Colore per i pulsanti -->
    <Color x:Key="ButtonBackgroundColor">#FF2D2D30</Color>
    <Color x:Key="ButtonForegroundColor">#FFF1F1F1</Color>

    <!-- Brush (pennelli) associati ai colori -->
    <SolidColorBrush x:Key="WindowBackgroundBrush" Color="{StaticResource BackgroundColor}" />
    <SolidColorBrush x:Key="WindowForegroundBrush" Color="{StaticResource ForegroundColor}" />

    <SolidColorBrush x:Key="ButtonBackgroundBrush" Color="{StaticResource ButtonBackgroundColor}" />
    <SolidColorBrush x:Key="ButtonForegroundBrush" Color="{StaticResource ButtonForegroundColor}" />

    <!-- Eventuali stili personalizzati -->
    <Style TargetType="Button">
        <Setter Property="Background" Value="{StaticResource ButtonBackgroundBrush}" />
        <Setter Property="Foreground" Value="{StaticResource ButtonForegroundBrush}" />
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Margin" Value="5"/>
        <Setter Property="Padding" Value="8,4"/>
    </Style>

    <Style TargetType="Window">
        <Setter Property="Background" Value="{StaticResource WindowBackgroundBrush}" />
        <Setter Property="Foreground" Value="{StaticResource WindowForegroundBrush}" />
    </Style>

</ResourceDictionary>
